import React from 'react'
import { connect } from 'react-redux'

export const ChooseDialect = (props) => {
  return (
    <>
      
    </>
  )
}

const mapStateToProps = (state) => ({
  
})

export default connect(mapStateToProps, mapDispatchToProps)(ChooseDialect)
